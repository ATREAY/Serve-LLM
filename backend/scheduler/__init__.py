# Phase 6-7: request queueing, priority, and continuous-batching experiments
# on top of vLLM's own batching. Not wired in Phase 1 — the gateway calls the
# engine directly and lets vLLM's internal scheduler handle batching.
